# TESA_3.1
````markdown
# TESA — платформа для анализа тональности текстов

TESA — это полный стек для анализа тональности на русском языке:  
**FastAPI-бэкенд** с моделью, **React + TypeScript фронтенд** с удобным интерфейсом,  
поддержка CSV-датасетов, метрик качества, ручного анализа, лемматизации и NER.

Проект спроектирован так, чтобы:

- легко **докинуть свой бэкенд** (другой хост / порт),
- **разворачивать фронт на любом домене** (Netlify, Vercel, свой сервер),
- управлять подключением к API **через UI**, без перекомпиляции.

---

## 📚 Содержание

1. [Общая архитектура](#-общая-архитектура)
2. [Функциональность фронтенда](#-функциональность-фронтенда)
3. [Функциональность бэкенда](#-функциональность-бэкенда)
4. [Форматы файлов и данных](#-форматы-файлов-и-данных)
5. [Установка и запуск](#-установка-и-запуск)
   - [Запуск бэкенда](#запуск-бэкенда)
   - [Запуск фронтенда](#запуск-фронтенда)
6. [Настройка подключения к бэкенду через интерфейс](#-настройка-подключения-к-бэкенду-через-интерфейс)
   - [Как это устроено в коде](#как-это-устроено-в-коде)
   - [Типичные сценарии переключения хоста/порта](#типичные-сценарии-переключения-хостапорта)
7. [Страницы и сценарии использования](#-страницы-и-сценарии-использования)
   - [Анализ CSV](#анализ-csv)
   - [Результаты и разметка](#результаты-и-разметка)
   - [Оценка качества (метрики)](#оценка-качества-метрики)
   - [Визуализации](#визуализации)
   - [Ручной анализ текста](#ручной-анализ-текста)
   - [Настройки](#настройки)
8. [Работа с CORS и развёртывание на другом домене](#-работа-с-cors-и-развёртывание-на-другом-домене)
9. [Расширение и кастомизация](#-расширение-и-кастомизация)
10. [Лицензия](#-лицензия)

---

## 🧠 Общая архитектура

Высокоуровневая схема:

```text
+--------------------+         HTTP (JSON/CSV)          +------------------------+
|     Frontend       |  <---------------------------->  |         Backend        |
| React + TS + Vite  |                                  | FastAPI + PyTorch      |
| (Netlify / любой   |                                  | /predict, /csv,        |
|  другой хостинг)   |                                  | /lemmatize, /ner,      |
+--------------------+                                  | /health                |
                                                        +------------------------+
````

* Фронт и бэк могут жить **на разных хостах/доменах**.
* Связь настраивается через UI (host/port бэкенда) + CORS на бэке.
* Возможна работа через обратный прокси `/api/...` (например, Netlify → VPS).

---

## 💻 Функциональность фронтенда

Фронтенд даёт удобный интерфейс для полного ML-пайплайна:

* **Работа с CSV-датасетом:**

  * загрузка CSV с отзывами,
  * отправка его на бэкенд,
  * получение предсказанных меток,
  * просмотр и правка разметки.

* **Фильтрация и поиск:**

  * фильтр по тональности (0 / 1 / 2 → negative / neutral / positive),
  * фильтр по источнику `src`,
  * фильтр по статусу строки (исправлена/не исправлена),
  * поиск по тексту или по `src`.

* **Экспорт:**

  * выгрузка результата в CSV с кастомными колонками (`ID`, `text`, `label`).

* **Оценка качества:**

  * подсчёт **macro-F1**,
  * метрики по классам (precision/recall/F1),
  * **confusion matrix**,
  * отчёт по метрикам в CSV.

* **Ручной анализ текста:**

  * ввод текста,
  * запрос к `/predict`,
  * опционально лемматизация (`/lemmatize`) и NER (`/ner`),
  * отображение отредактированного текста (если включена лемматизация),
  * список найденных сущностей в таблице.

* **Настройки:**

  * выбор темы (**light / dark**),
  * включение/выключение отдельных визуализаций,
  * настройка **host/port** бэкенда через UI,
  * настройка маппинга классов (`labelMapping`) — какой код чему соответствует.

UI стилизован под **современный “liquid glass + neon”** стиль, напоминающий интерфейс новых iOS: мягкие тени, glassmorphism, неоновые акценты.

---

## 🧾 Функциональность бэкенда

Бэкенд — сервис инференса тональности и лингвистических фич на базе FastAPI.

### Основные зависимости

* `fastapi`
* `uvicorn`
* `torch`
* `pandas`
* `pyyaml`
* собственный `ModelWrapper` для загрузки модели

### Конфиг `config.yaml`

Используется переменная окружения `SENTIMENT_CONFIG_PATH`
(по умолчанию `/app/config.yaml`).

Пример:

```yaml
model_path: "/app/model"
prefer_cuda: true
enable_compile: false
host: "0.0.0.0"
port: 51000
csv_batch_size: 64
log_level: "INFO"
log_format: "plain"
```

Полный Pydantic-класс:

```python
class ServiceConfig(BaseModel):
    model_path: str
    prefer_cuda: bool = True
    enable_compile: bool = False
    host: str = "0.0.0.0"
    port: int = 8000
    csv_batch_size: int = 64
    log_level: str = "INFO"
    log_format: Literal["plain", "json"] = "plain"
```

### CORS

Внутри `create_app()` добавлен CORS-middleware:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=False,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

Это значит, что бэкенд принимает запросы с **любой origin** (под хакатон/демо это ок).

### Эндпоинты

#### `POST /predict`

Одиночный текст.

* **Request (JSON)**

```json
{
  "text": "Отзыв пользователя..."
}
```

* **Response (JSON)**

```json
{
  "prediction": 2,
  "negative_score": 1.2345,
  "neutral_score": 5.6789,
  "positive_score": 92.3456
}
```

Здесь `prediction` — код класса (0/1/2),
а `*_score` — проценты для каждого класса.

#### `POST /csv`

Пакетный анализ CSV.

* **Формат входного CSV**:

  * обязательные колонки: `ID`, `text`
  * пример:

    ```csv
    ID,text
    83412,"Отличное приложение, работает супер быстро"
    12907,"Ужасно медленное приложение, постоянно глючит"
    ```

* **Request (multipart/form-data)**

Поля:

* `file` — бинарный CSV-файл.

* **Ответ** — стрим CSV:

  ```csv
  ID,label
  83412,2
  12907,0
  ```

#### `POST /lemmatize`

* **Request (JSON)**

```json
{
  "text": "Отличное приложение, работает супер быстро"
}
```

* **Response (JSON)**

```json
{
  "output": "отличный приложение работать супер быстро"
}
```

(структура соответствует схеме `LemmatizeResponse` из OpenAPI)

#### `POST /ner`

* **Request (JSON)**

```json
{
  "text": "Сбербанк запустил новую акцию в Москве"
}
```

* **Response (JSON)**

```json
{
  "entities": [
    "Сбербанк",
    "Москве"
  ]
}
```

(структура соответствует `NerResponse`)

#### `GET /health`

Простой статус сервиса:

```json
{
  "status": "ok",
  "device": "cuda:0",
  "compiled": false,
  "attention": "sdpa"
}
```

---

## 📂 Форматы файлов и данных

### 1. Входной файл для анализа (frontend → backend `/csv`)

**Обязательные колонки:**

* `ID` — уникальный идентификатор строки (число или строка),
* `text` — текст отзыва.

Пример:

```csv
ID,text
83412,"Отличное приложение, работает супер быстро"
12907,"Ужасно медленное приложение, постоянно глючит"
```

### 2. Файл с предсказаниями (backend → frontend после `/csv`)

```csv
ID,label
83412,2
12907,0
...
```

Это CSV фронт обратно сливает с исходными текстами по `ID`, чтобы строить таблицу.

### 3. Файл эталонной разметки для метрик (frontend → AnalysisContext → metrics)

Файл валидации (для macro-F1, confusion matrix):

* обязательная колонка: `label`
* дополнительные: `ID` и/или `text` (лучше хотя бы одно из них)

Типичный вариант:

```csv
ID,text,label
83412,"Отличное приложение, работает супер быстро",2
12907,"Ужасно медленное приложение, постоянно глючит",0
...
```

Сопоставление:

* если есть `ID` — матч по `ID`,
* если нет `ID`, но есть `text` — матч по нормализованному тексту,
* если нет ни `ID`, ни `text` — матч по порядку строк (fallback).

---

## 🛠 Установка и запуск

### Запуск бэкенда

1. Установить зависимости:

   ```bash
   pip install -r requirements.txt
   ```

2. Подготовить `config.yaml`:

   ```yaml
   model_path: "/app/model"
   prefer_cuda: true
   enable_compile: false
   host: "0.0.0.0"
   port: 51000
   csv_batch_size: 64
   log_level: "INFO"
   log_format: "plain"
   ```

3. Запустить сервис:

   ```bash
   export SENTIMENT_CONFIG_PATH=/app/config.yaml
   python service.py
   # или напрямую через uvicorn:
   uvicorn service:app --host 0.0.0.0 --port 51000
   ```

4. Проверить здоровье:

   ```bash
   curl http://<host>:51000/health
   ```

### Запуск фронтенда

1. Установить зависимости:

   ```bash
   npm install
   ```

2. Запустить dev-сервер:

   ```bash
   npm run dev
   ```

3. Открыть в браузере:

   ```text
   http://localhost:5173
   ```

4. Перейти на вкладку **«Настройки»** и указать корректный `host`/`port` бэкенда.

---

## ⚙️ Настройка подключения к бэкенду через интерфейс

### Как это устроено в коде

В `src/context/SettingsContext.tsx`:

```ts
export interface BackendSettings {
  host: string;
  port: number;
}

export interface AppSettings {
  ...
  backend: BackendSettings;
}
```

Дефолт:

```ts
const defaultSettings: AppSettings = {
  ...
  backend: {
    host: '5.129.212.83', // пример
    port: 51000,          // пример
  },
};
```

Функция сборки URL:

```ts
export const buildBackendBaseUrl = (settings: AppSettings): string =>
  `http://${settings.backend.host}:${settings.backend.port}`;
```

Все запросы в `sentimentApi.ts` идут через неё:

```ts
const baseUrl = buildBackendBaseUrl(settings);

await fetch(`${baseUrl}/predict`, ...);
await fetch(`${baseUrl}/csv`, ...);
await fetch(`${baseUrl}/lemmatize`, ...);
await fetch(`${baseUrl}/ner`, ...);
```

Настройки хранятся в `localStorage`:

```ts
const STORAGE_KEY = 'tesa_settings_v1';
```

При изменении через UI:

* `updateSettings({ backend: { host, port } })` обновляет контекст,
* новая конфигурация автоматически **подхватывается всеми запросами**,
* значения сохраняются между перезагрузками страницы.

### Типичные сценарии переключения хоста/порта

#### 1. С локального бэка на удалённый

* Локальный: `host = localhost`, `port = 8000`
* Прод: `host = 5.129.212.83`, `port = 51000`

В настройках:

1. Перейти на страницу **«Настройки»**.
2. Ввести новый `host` и `port`.
3. Сохранить (или просто уйти со страницы, если сохранение автоматически).
4. Вернуться во вкладку **«Анализ»** и запустить пайплайн.

#### 2. Работа через обратный прокси `/api`

Если фронт развернут, например, на Netlify, а бэк — на VPS, можно настроить:

* Фронт обращается к относительным путям `/api/csv`, `/api/predict` и т.д.
* Netlify (или Nginx) проксирует `/api/*` на `http://<backend_host>:<backend_port>`.

Пример `_redirects` для Netlify:

```text
/api/*  http://5.129.212.83:51000/:splat  200
```

Тогда фронтенд может просто использовать `fetch('/api/csv', ...)` и вообще не знать IP/порт бэка.
При этом описанный в проекте механизм `host/port` остаётся запасным/универсальным вариантом — можно выбирать стратегию в зависимости от среды.

---

## 📊 Страницы и сценарии использования

### Анализ CSV

Вкладка: **«Анализ»**

* Зона дропа для CSV:

  * принимает `ID,text`,
  * показывает название файла и кол-во строк.
* Кнопка **«Запустить анализ»**:

  * отправляет CSV на бэкенд,
  * сбрасывает состояние валидации/метрик,
  * обновляет таблицу результатов.
* Состояние джоба:

  * статус (`обработка`, `готово`, `ошибка`),
  * количество строк.

### Результаты и разметка

Вкладка: **«Результаты»**

* Таблица с результатами:

  * `ID`, `text`, предсказанный label, (при наличии) истинный label, исправленный label.
  * визуальная подсветка тональности (badges 0/1/2).
* Фильтры:

  * по тональности (chip-кнопки: 0/1/2),
  * по `src`,
  * по статусу (исправленные / не исправленные / все).
* Поиск:

  * по тексту или по `src` (переключатели режима поиска).
* Экспорт:

  * кнопка **«Скачать CSV»**,
  * чекбоксы `ID` и `text` — какие колонки добавлять к `label`.

### Оценка качества (метрики)

Вкладка: **«Оценка качества»**

1. **Блок загрузки эталонного CSV**:

   * drag&drop + кнопка выбора,
   * статус: последний файл, число сопоставленных строк,
   * подсказки, как готовить файл.

2. **Macro-F1 и summary**:

   * крупным шрифтом macro-F1,
   * подписи с пояснением, что значит это число,
   * chip-метрики по числу строк,
   * кнопка **«Скачать отчёт (CSV)»**.

3. **Метрики по классам**:

   * карточки для каждого класса:

     * F1,
     * precision,
     * recall,
     * мини-бар-чарты.

4. **Confusion matrix**:

   * если включена в настройках,
   * тепловая карта с процентами и absolute count.

### Визуализации

Вкладка: **«Визуализации»**

* Возможные графики (включаются/выключаются в настройках):

  * распределение тональностей,
  * разбивка по источникам `src`,
  * bar-chart по F1.

(Конкретная реализация может меняться, но общая идея — обзор “здоровья” модели и датасета.)

### Ручной анализ текста

Компонент: **ManualAnalysisCard**

* textarea для текста;
* кнопка **«Проанализировать»**:

  * вызывает `/predict`,
  * при включённой лемматизации — добавляет результат `/lemmatize`,
  * при включенном NER — добавляет `/ner`;
* чекбоксы в одной строке с кнопками:

  * **«Лемматизация»** — если включен, в таблице показывается **отредактированный текст** (результат `/lemmatize`);
  * **«NER»** — если включен, в таблице появляется колонка со списком найденных сущностей;
* кнопка **«Очистить»** — сброс текста и истории запросов;
* таблица последних запросов (до 10):

  * # (номер),
  * текст / лемматизированный текст,
  * тональность с бейджем,
  * (опционально) сущности.

### Настройки

Вкладка: **«Настройки»**

* **Тема**:

  * переключатель `dark` / `light`,
  * тема применяется к `<html>` через CSS-классы `tesa-theme-dark` / `tesa-theme-light`.

* **Визуализации**:

  * чекбоксы:

    * `showSentimentDistribution`,
    * `showSourceBreakdown`,
    * `showConfusionMatrix`,
    * `showF1BarChart`.

* **Маппинг классов (`labelMapping`)**:

  * соответствие: `0 → negative`, `1 → neutral`, `2 → positive`,
  * важно, если нужно адаптировать под свою схему разметки.

* **Подключение к бэкенду**:

  * поля `host` и `port`,
  * при изменении сразу сохраняются и начинают использоваться всеми запросами.

---

## 🌐 Работа с CORS и развёртывание на другом домене

Благодаря:

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=False,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

бэкенд готов принимать запросы от:

* локального фронта (`http://localhost:5173`),
* Netlify (`https://*.netlify.app`),
* любого другого домена.

Условия:

* бэкенд должен быть доступен по сети (порт открыт снаружи),
* в интерфейсе фронта нужно указать корректный `host`/`port` (или настроить прокси `/api`),
* при необходимости можно сузить `allow_origins` до конкретных доменов.

---

## 🧩 Расширение и кастомизация

Идеи, как развивать проект дальше:

* **Больше моделей**: добавить выпадающий список выбора модели на фронте и маршрутизацию запросов на разные префиксы на бэке.
* **Аутентификация**: навесить JWT/ключи API на FastAPI, добавить ввод токена на фронте.
* **История проектов**: сохранять загруженные датасеты и результаты в базе, давать к ним доступ из UI.
* **Тонкая настройка лемматизации/NER**: выбор языка, моделей, отображения сущностей (разные типы: PER/ORG/LOC).
* **Экспорт отчётов** в PDF/HTML с визуализациями.

Архитектура уже разделена на чистые слои:

* бэкенд — только API и модель;
* фронт — только UI и визуализация.

Это делает дальнейшее развитие довольно прямолинейным.

---

## 📄 Лицензия

Укажи здесь выбранную лицензию (MIT / Apache-2.0 / проприетарная) и условия использования.

Например, для MIT:

```text
MIT License

Copyright (c) 2025 ...

Permission is hereby granted, free of charge, to any person obtaining a copy
...
```

---

```
::contentReference[oaicite:0]{index=0}
```
